apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: recommendation
type: Opaque
stringData:
  # Replace with actual base64 encoded values before applying
  AWS_ACCESS_KEY_ID: "REPLACE_WITH_YOUR_AWS_KEY"
  AWS_SECRET_ACCESS_KEY: "REPLACE_WITH_YOUR_AWS_SECRET"
  MLFLOW_DB_PASSWORD: "MLflowSecurePass123!"
